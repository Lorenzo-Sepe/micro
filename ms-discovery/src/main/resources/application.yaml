spring:
  application:
    name: ms-discovery

server:
  port: 8761

eureka:
  environment: default
  instance:
    hostname: localhost
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/

logging:
  level:
    org.springframework: debug
    it.cgmconsulting.discovery: debug

